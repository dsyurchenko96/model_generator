{
  "title": "Main Schema",
  "type": "object",
  "properties": {
    "kind": {
      "title": "Kind",
      "maxLength": 32,
      "minLength": 1,
      "type": "string"
    },
    "name": {
      "title": "Name",
      "maxLength": 128,
      "minLength": 1,
      "type": "string"
    },
    "description": {
      "title": "Description",
      "maxLength": 4096,
      "minLength": 1,
      "type": "string"
    },
    "version": {
      "title": "Version",
      "pattern": "^(?P<major>0|[1-9]\\d*)\\.(?P<minor>0|[1-9]\\d*)\\.(?P<patch>0|[1-9]\\d*)(?:-(?P<prerelease>(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+(?P<buildmetadata>[0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$",
      "type": "string"
    },
    "configuration": {
      "$ref": "#/definitions/SchemaConfigField"
    }
  },
  "required": [
    "kind",
    "name",
    "description",
    "version",
    "configuration"
  ],
  "additionalProperties": false,
  "definitions": {
    "SchemaConfigField": {
      "title": "Main Schema",
      "type": "object",
      "properties": {
        "specification": {
          "title": "Specification",
          "type": "object"
        },
        "settings": {
          "title": "Settings",
          "type": "object"
        }
      },
      "required": [
        "specification",
        "settings"
      ],
      "additionalProperties": false
    }
  }
}
